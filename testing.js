const cursor=document.querySelector('.cursor');window.addEventListener('pointermove',e=>{cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px'},{passive:true});const slides=[...document.querySelectorAll('.slide')],dots=[...document.querySelectorAll('.dot')];let index=0,timer;function show(next){index=(next+slides.length)%slides.length;slides.forEach((s,i)=>s.classList.toggle('active',i===index));dots.forEach((d,i)=>d.classList.toggle('active',i===index));document.querySelector('.current').textContent=String(index+1).padStart(2,'0')}function autoplay(){clearInterval(timer);timer=setInterval(()=>show(index+1),6500)}document.querySelector('.next').addEventListener('click',()=>{show(index+1);autoplay()});document.querySelector('.prev').addEventListener('click',()=>{show(index-1);autoplay()});dots.forEach((d,i)=>d.addEventListener('click',()=>{show(i);autoplay()}));document.querySelector('[data-go]').addEventListener('click',()=>document.querySelector('.showcase').scrollIntoView({behavior:'smooth'}));autoplay();let startX=0;document.querySelector('.slider').addEventListener('touchstart',e=>startX=e.changedTouches[0].screenX,{passive:true});document.querySelector('.slider').addEventListener('touchend',e=>{const dx=e.changedTouches[0].screenX-startX;if(Math.abs(dx)>45)show(index+(dx<0?1:-1));autoplay()},{passive:true});document.querySelector('.menu').addEventListener('click',()=>alert('Use the section links in the desktop navigation.'));
