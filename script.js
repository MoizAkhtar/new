const menu=document.querySelector('.menu');const nav=document.querySelector('.nav nav');menu?.addEventListener('click',()=>nav.classList.toggle('open'));const progress=document.querySelector('.progress span');window.addEventListener('scroll',()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${max?scrollY/max*100:0}%`},{passive:true});const glow=document.querySelector('.cursor-glow');window.addEventListener('pointermove',e=>{glow.style.left=`${e.clientX}px`;glow.style.top=`${e.clientY}px`},{passive:true});const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const scenes=[];const colors={lime:0x62f5ff,pink:0x816bff,cyan:0x6fe6d3,blue:0x3478ff,ink:0xf5f8ff};function setupScene(el,type){if(!window.THREE)return;const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(38,1,.1,100),renderer=new THREE.WebGLRenderer({alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));el.appendChild(renderer.domElement);camera.position.z=5;let object;const material=(color=colors.lime,wire=true)=>new THREE.MeshBasicMaterial({color,wireframe:wire,transparent:true,opacity:.86});if(type==='torus')object=new THREE.Mesh(new THREE.TorusKnotGeometry(1.2,.34,128,20),material(colors.lime));else if(type==='cube')object=new THREE.Mesh(new THREE.IcosahedronGeometry(1.35,1),material(colors.ink));else if(type==='scan')object=new THREE.Mesh(new THREE.SphereGeometry(1.35,24,16),material(colors.cyan));else if(type==='knot')object=new THREE.Mesh(new THREE.TorusKnotGeometry(1.35,.42,120,16),material(colors.pink));else if(type==='network'){object=new THREE.Group();for(let i=0;i<22;i++){const node=new THREE.Mesh(new THREE.SphereGeometry(.06,8,8),new THREE.MeshBasicMaterial({color:colors.lime}));node.position.set((Math.random()-.5)*3,(Math.random()-.5)*3,(Math.random()-.5)*2);object.add(node)}}else object=new THREE.Mesh(new THREE.SphereGeometry(1.45,24,16),material(colors.blue));scene.add(object);const ring=new THREE.Mesh(new THREE.TorusGeometry(1.8,.012,8,80),new THREE.MeshBasicMaterial({color:colors.lime,transparent:true,opacity:.35}));ring.rotation.x=1.1;scene.add(ring);const resize=()=>{const r=el.getBoundingClientRect();renderer.setSize(Math.max(r.width,1),Math.max(r.height,1),false);camera.aspect=r.width/Math.max(r.height,1);camera.updateProjectionMatrix()};resize();new ResizeObserver(resize).observe(el);scenes.push({object,ring,renderer,scene,camera,el,type,phase:Math.random()*8})}document.querySelectorAll('.scene').forEach(el=>setupScene(el,el.dataset.model||el.parentElement?.dataset.scene||'sphere'));function animate(t){requestAnimationFrame(animate);scenes.forEach(s=>{const r=s.el.getBoundingClientRect(),active=r.top<innerHeight&&r.bottom>0;s.object.rotation.x+=active?.003:0;s.object.rotation.y+=active?.006:0;s.ring.rotation.z+=active?.002:0;s.object.position.y=Math.sin(t*.001+s.phase)*.12;s.renderer.render(s.scene,s.camera)})}animate(0);
